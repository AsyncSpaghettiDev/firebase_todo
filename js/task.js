// Clase para crear una tareas que se llama en app.js
class Task {
  constructor (title, dueDate, description, tag, color, category, important) {
    this.title = title
    this.dueDate = dueDate
    this.description = description
    this.tag = tag
    this.color = color
    this.category = category
    this.isImportant = important

    this.name = "Equipo 2"
  }
}
